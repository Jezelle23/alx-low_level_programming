#include "main.h"
/**
 * mul - checks for checks for a digit (0 through 9)
 * @a: variable
 * @b: variable
 * Return: Always 0 (success)
 */
int mul(int a, int b)
{

	int mul;

	mul = a * b;

	return (mul);
}
